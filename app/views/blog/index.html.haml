#backnumber
  %a{:href => "/backnumber"} バックナンバーを見る &raquo;
- @entries.each do |entry|
  %article.index
    .article_inner
      .content
        %header
          %h2
            %a{:href => "#{entry.rel_url}"} #{entry.title}
          .meta
            %span.date published at #{entry.date}
        %p
          #{raw entry.summary}
